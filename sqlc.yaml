version: "2"
sql:
  - schema: schema.sql
    queries: query.sql
    engine: postgresql
    gen:
      go:
        out: assets/db
        emit_json_tags: true
        emit_db_tags: true
        sql_package: pgx/v5
        emit_pointers_for_null_types: true
